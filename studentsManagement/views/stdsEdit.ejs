<!doctype html>
<html lang="ar" dir="rtl">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		
		<title><%=locals.title ? title:"الرئيسية"%></title>
		
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
		
		<link href="/css/bootstrap.rtl.min.css" rel="stylesheet">
		<link href="/css/style.css" rel="stylesheet">
		
		<noscript>
		يرجى تفعيل خاصية JavaScript في المتصفح ، لمتابعة استخدام هذا التطبيق.
		</noscript>
	</head>
	<body>
		<% if(locals.stds && stds.length > 0){%>
			<% stds.forEach(std =>{%>
				<% if(std._id == idStd) {%>
					<div class="container" style="margin-top:120px;>
						<div class="row">
							<div class="col">
								<h1 class="text-center mb-4">
									الـتعديـل على الـطالب(ة) 
									<strong style="color:#fb0a0a"><%=std.name%></strong>
								</h1>
							</div>
							<div class="col-md-4 mx-auto">
								<form class="mt-4" 
									action="/update/<%=std._id%>?_method=put" method="post">
									<div class="input-group">
									<input type="text" class="form-control" name="name" 
									value="<%=std.name%>">
									</div>
									<div class="input-group mb-2">
									<input type="text" class="form-control" name="age" 
									value="<%=std.age%>">
									</div>
									<!-- cancel changes -->
									<a class="btn btn-warning" role="button" 
										href="/" title="Cancel">
										<i class="fa-solid fa-xmark"></i>
									</a>
									<!-- accept changes -->
									<button type="submit" class="btn 
										btn-warning" title="Validate">
										<i class="fa-regular fa-paper-plane"></i>
									</button>
								</form>
							</div>
						</div>
					</div><!-- /.container -->
				<%} %>
			<%}) %>
		<%} %>
	</body>
</html>